# 1. Cek versi minimal CMake yang dibutuhkan
cmake_minimum_required(VERSION 3.10)

# 2. Beri nama proyekmu
project(LatihanPertama)

# 3. Perintah utama: Masak 'main.c' menjadi executable bernama 'aplikasiku'
add_executable(aplikasiku main.c)
