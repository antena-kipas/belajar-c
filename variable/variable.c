#include <stdio.h>

int main(){
  int cc = 120;
  float kecepatan = 100.9;
  char grade = 'H';

  printf("motor ini memeliki cc dengan jumlah: %d cc\n", cc);
  printf("dan motor ini memiliki kecepatan: %.1f km/jam\n", kecepatan);
  printf("grade motor: %c\n", grade);
  return 0;
}
